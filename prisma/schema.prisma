generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// Dummy model so the client generates successfully
model User {
  id    String @id @default(uuid())
  email String @unique
  name  String?
}